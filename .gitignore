.DS_Store
target
node_modules/
runtime/*.node
runtime/index.js
runtime/index.d.ts
.playwright-mcp

# Local app config. Check in brust.example.toml; ignore brust.toml.
brust.toml

# Brust build output
.brust/
dist
.antigravitycli

# Declaration emit for the published package (`bun run build:dts`; regenerated
# by prepack). Root-anchored so app-level types/ dirs are unaffected.
/types/

# Unrelated local spike — kept on disk, never tracked (swept into #20 by accident)
tools/jsx-rust-compiler
pokedex.bak.*
