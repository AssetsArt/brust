import { Island } from 'brustjs'
import Layout from '../components/Layout'
import Counter from '../components/Counter'

export default function Home() {
  return (
    <Layout title="__PROJECT_NAME__">
      <h1 className="text-3xl font-bold mb-4">Welcome to Brust</h1>
      <p className="mb-6 text-gray-700">
        Edit <code className="bg-gray-100 px-1 rounded">pages/Home.tsx</code> and save —
        <code className="bg-gray-100 px-1 rounded ml-1">brustjs dev</code> reloads.
      </p>

      <section className="mb-6">
        <h2 className="text-xl font-semibold mb-1">Client island</h2>
        <p className="mb-2 text-sm text-gray-600">
          Ships no server markup; mounts and hydrates in the browser.
        </p>
        <Island component={Counter} props={{ start: 0, label: 'client clicks' }} hydrate="load" />
      </section>

      <section>
        <h2 className="text-xl font-semibold mb-1">Server island (SSR + hydrate)</h2>
        <p className="mb-2 text-sm text-gray-600">
          Rendered on the server, then hydrated — interactive with no flash of empty state.
        </p>
        <Island
          component={Counter}
          props={{ start: 100, label: 'server clicks' }}
          hydrate="load"
          ssr
        />
      </section>
    </Layout>
  )
}
