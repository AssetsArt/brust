{
  "name": "brust-runtime",
  "type": "module",
  "private": true,
  "main": "index.js",
  "types": "index.d.ts",
  "scripts": {
    "build": "napi build --platform --release --js index.js --dts index.d.ts --package-json-path ../package.json --manifest-path ../crates/brust/Cargo.toml --output-dir .",
    "build:debug": "napi build --platform --js index.js --dts index.d.ts --package-json-path ../package.json --manifest-path ../crates/brust/Cargo.toml --output-dir ."
  },
  "devDependencies": {
    "@napi-rs/cli": "^3.0.0",
    "typescript": "^5.5.0"
  }
}
